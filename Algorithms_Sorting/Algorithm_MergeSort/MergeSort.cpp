#include <vector>

std::vector<int> Sort(std::vector<int> A, std::vector<int> B)
{
    std::vector<int> solution(A.size() + B.size());

    

    return solution;
}

int main()
{

    return 0;
    
}